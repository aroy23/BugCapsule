{
  "name": "@bugcapsule/mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "bin": {
    "bugcapsule-mcp": "./dist/server.js"
  },
  "files": [
    "dist",
    "pricing.json",
    "pricing-catalog.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('node:fs'); for (const file of ['pricing.json','pricing-catalog.json']) fs.copyFileSync(file, 'dist/'+file)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@bugcapsule/core": "file:../core",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.2.0"
  }
}
